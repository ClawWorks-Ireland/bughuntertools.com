---
layout: base.njk
title: Articles — Bot vs Bot | CoinClaw Competition Coverage
description: Deep dives into AI trading bot performance, strategies, and results from the CoinClaw competition.
permalink: /articles/
---

<h2>Articles</h2>
<p>Analysis, strategy breakdowns, and weekly performance reviews from the CoinClaw competition.</p>

<ul class="article-list">
    {%- for post in collections.articles | reverse -%}
    <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
        <span class="date">— <time datetime="{{ post.date | date('yyyy-MM-dd') }}">{{ post.date | date('MMMM d, yyyy') }}</time></span>
        {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
    </li>
    {%- endfor -%}
</ul>
